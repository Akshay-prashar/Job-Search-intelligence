import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "JobIntel — Smart Job Matching for CS Freshers",
  description:
    "Discover, match, and track entry-level tech jobs with AI-powered resume parsing, hybrid scoring, and explainable recommendations. Built for CS freshers.",
  keywords: [
    "job search",
    "fresher jobs",
    "CS jobs",
    "entry level",
    "AI matching",
    "resume parser",
    "job recommendations",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} dark`}
    >
      <body className="min-h-screen bg-[var(--background)] text-[var(--foreground)] antialiased">
        {children}
      </body>
    </html>
  );
}
